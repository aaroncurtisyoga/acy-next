import BasicInfo from "@/app/admin/events/_components/EventForm/Steps/BasicInfo";

/**
 * Edit event - Basic Info step
 * Data is fetched once in the layout and passed via EventFormProvider context
 */
export default function EditBasicInfoPage() {
  return <BasicInfo />;
}
